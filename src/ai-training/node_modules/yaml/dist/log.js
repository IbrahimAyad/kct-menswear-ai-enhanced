'use strict';

var node_process = require('process');

function debug(logLevel, ...messages) {
    if (logLevel === 'debug')
        else
            console.warn(warning);
    }
}

exports.debug = debug;
exports.warn = warn;
